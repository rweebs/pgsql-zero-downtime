apiVersion: v1
kind: Service
metadata:
  name: app
  annotations:
    linkerd.io/inject: enabled
spec:
  type: LoadBalancer
  ports:
    - name: app
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: ${service_label}
    tier: backend